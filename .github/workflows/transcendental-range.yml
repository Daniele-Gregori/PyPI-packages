name: transcendental-range

on:
  push:
    paths:
      - "packages/transcendental-range/**"
      - ".github/workflows/transcendental-range.yml"
  pull_request:
    paths:
      - "packages/transcendental-range/**"
      - ".github/workflows/transcendental-range.yml"
  workflow_dispatch:

defaults:
  run:
    working-directory: packages/transcendental-range/

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      # The randomized TimeConstrained suite (test_transcendental_range.py)
      # is skipped on CI and run locally; CI runs the deterministic
      # documentation-example suite.
      - name: Run tests
        run: pytest tests/test_documentation_examples.py -v
